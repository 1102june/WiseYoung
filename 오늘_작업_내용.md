# 오늘 작업 내용 (2025-11-22)

## 🎨 UI 개선

### 1. Splash 화면 수정
- 배경색 변경: `#f79659` (단색)
- WelcomeActivity 제거 → AuthActivity로 직접 이동
- 로그인된 사용자는 MainActivity로 이동

### 2. 회원가입 화면
- 재발송 버튼 크기: `56.dp` → `64.dp`

### 3. 로그인 화면
- Placeholder 텍스트 크기: `14.sp` → `12.sp`

## 🎯 테마 시스템 개선

### 1. 테마 적용 범위 확대
- 모든 Activity에 `ThemeWrapper` 적용
  - LoginActivity, RegisterActivity, CompleteActivity
  - AuthActivity, ProfileSetupActivity, PasswordResetActivity
  - CalendarActivity, BookmarkActivity, NotificationActivity
  - PolicyListActivity, HousingMapActivity, ProfileActivity

### 2. 테마 변경 기능 수정
- `ThemePreferences.setThemeMode()`: `apply()` → `commit()` (동기 저장)
- `ThemeWrapper`: SharedPreferences 변경 감지 및 자동 업데이트
- `PREFS_NAME`, `KEY_THEME_MODE`: `internal`로 변경하여 접근성 개선

## 🐛 버그 수정

### 1. 컴파일 오류 해결
- `WiseYoungTheme` 오버로드 모호성 해결
  - Boolean 파라미터 기본값 제거 (필수 파라미터로 변경)
- `ProfileActivity`에서 존재하지 않는 `themeMode` 변수 참조 제거

## 📝 기술적 변경사항

- `ThemeWrapper`: `LaunchedEffect` 추가로 Activity 재시작 시 최신 테마 자동 로드
- `DisposableEffect`: SharedPreferences 변경 리스너로 실시간 테마 업데이트


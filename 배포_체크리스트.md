# 안드로이드 앱 배포 체크리스트

## ⚠️ 배포 전 필수 수정 사항

### 1. NetworkModule.kt 수정 필요

`app/src/main/java/com/example/app/network/NetworkModule.kt` 파일 수정:

```kotlin
object NetworkModule {
    // ⚠️ 배포 서버 URL로 변경 필수!
    // 개발 서버 IP 또는 도메인으로 변경
    private const val BASE_URL = "http://서버IP:8080/"  // 예: "http://123.456.789.0:8080/"
    // 또는 도메인 사용 시
    // private const val BASE_URL = "https://api.yourdomain.com/"
    
    // 프로파일별로 분리하려면:
    // BuildConfig.DEBUG에 따라 자동 전환 가능
    // private const val BASE_URL = if (BuildConfig.DEBUG) 
    //     "http://127.0.0.1:8080/" 
    // else 
    //     "http://서버IP:8080/"
    
    // ... 나머지 코드 동일
}
```

### 2. 빌드 타입별 Base URL 분리 (권장)

`app/build.gradle.kts`에 BuildConfig 필드 추가:

```kotlin
android {
    buildTypes {
        debug {
            buildConfigField("String", "BASE_URL", "\"http://127.0.0.1:8080/\"")
        }
        release {
            buildConfigField("String", "BASE_URL", "\"http://서버IP:8080/\"")
            isMinifyEnabled = false
        }
    }
}

// buildFeatures에 buildConfig 추가
buildFeatures {
    compose = true
    buildConfig = true  // 추가
}
```

그리고 `NetworkModule.kt`에서 사용:

```kotlin
private const val BASE_URL = BuildConfig.BASE_URL
```

---

## ✅ 배포 전 확인 사항

### 기능 점검
- [ ] 로그인 기능 정상 동작
- [ ] 정책 목록 조회 정상
- [ ] 임대주택 목록 조회 정상
- [ ] 북마크 기능 정상
- [ ] 캘린더 기능 정상
- [ ] 카카오맵 표시 정상
- [ ] 챗봇 기능 정상

### 설정 점검
- [ ] Base URL 변경 완료
- [ ] Firebase 설정 확인 (`google-services.json`)
- [ ] 카카오맵 키 해시 등록 확인
- [ ] 프로가드 설정 (release 빌드 시)

### 보안 점검
- [ ] API 키가 하드코딩되지 않음
- [ ] 민감한 정보 제거
- [ ] 디버그 로그 비활성화 (release)

---

## 📦 Release 빌드 생성

```bash
cd C:\Users\subpa\StudioProjects\WiseYoung
.\gradlew clean
.\gradlew assembleRelease

# 빌드된 APK 위치
# app/build/outputs/apk/release/app-release.apk
```

---

## 🔄 업데이트 시 주의사항

1. **버전 코드 증가**: `build.gradle.kts`에서 `versionCode` 증가
2. **버전 이름 변경**: `versionName` 업데이트
3. **변경 사항 테스트**: 모든 기능 재테스트
4. **서버 호환성 확인**: 백엔드 API 변경 사항 확인

---

## 📱 배포 후 확인

1. 서버 연결 확인
2. 모든 API 호출 정상 동작
3. Firebase 인증 정상
4. 카카오맵 로드 정상
5. 사용자 피드백 모니터링

